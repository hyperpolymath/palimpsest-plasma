// SPDX-License-Identifier: CC-BY-SA-4.0
// Copyright (c) Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= Palimpsest Plasma — Roadmap
:toc:
:toc-placement!:
:sectnums:

toc::[]

== Current Status

**v0.2.0**: the repository compiles, all tests pass, and engine v0 is real —
a typed policy AST, versioned schema loading, deterministic fact collection,
a pure/total evaluator, and a CLI (`plasma check/facts/policy/audit`) that
this repository applies to itself in CI via its bundled repo-hygiene policy.

The project's identity settled here too: plasma is a deterministic policy
engine for the agentic era. The Palimpsest license (PMPL) is a separate
future project; the earlier union-policy-parser sub-project was removed
(it lives on in git history and belongs in its own repository).

== Completed Milestones

=== v0.2.0 — Truth + Green + Engine v0 [COMPLETE]

* [x] Repository compiles; full test suite green
* [x] License unification: code MPL-2.0, docs CC-BY-SA-4.0, SPDX headers swept
* [x] plasma-engine crate: Policy AST, schema load/validate, facts, evaluator
* [x] Deontic semantics (obligation/prohibition/permission) with severity
* [x] Bundled repo-hygiene policy; self-test gate in CI
* [x] `plasma check` (human/JSON/SARIF), `plasma facts`, `plasma policy validate`
* [x] `plasma audit` rewired onto plasma-parser (zone-aware, real SPDX parsing)
* [x] init/migrate/badge genericized to any SPDX license
* [x] Docs truth pass: README, EXPLAINME, TOPOLOGY, architecture, engine-v0-design
* [x] union-policy-parser removed; corrupted license artifacts removed

== Active Milestones

=== v0.3.0 — Action Planner [IN PROGRESS]

* [x] `plasma fix` — plan (dry-run) and apply modes, with `.bak` backups
* [x] Findings → planned actions mapping (pure `plan`; IO `apply`)
* [x] First action type: `AddSpdxHeader` (+ manual partition with reasons)
* [x] Action type `CreateFile` (v0.6) — create a missing required file at a
      concrete path; `UpdateField` and others still to come
* [x] Overlay `override-rules` semantics (v0.5) — remove/replace base rules
      per context; `modify-rules` remains reserved
* [x] Content facts + `file-matches-pattern` condition semantics (v0.6) —
      opt-in `file_contents`, regex validated at load

=== v0.4.0 — Interchange Contracts & Integration Surfaces [IN PROGRESS]

* [x] Sibling-tool contracts pinned: facts snapshot, facts diff, findings,
      SARIF namespace (docs/interchange-contracts.adoc)
* [x] `plasma diff before.json after.json` — deterministic facts diff with
      diff(1) exit semantics (0 identical / 1 differ / 2 error)
* [ ] Git hooks (pre-commit, pre-push) via `plasma check --quiet`
* [ ] Reference GitHub Action / GitLab CI job
* [ ] `.plasma.toml` policy references (project-pinned policies)
* [ ] Release facts (`release` subjects/resources gain semantics)

== Future Directions

* Formal core: Catala/OCaml implementation of the evaluation semantics,
  verified against docs/engine-v0-design.adoc (the format is specified for
  exactly this)
* WASM compilation target for browser-based policy checks
* Decision registries (`consistent-with` action semantics)
* Multi-repo fleet evaluation
* Policy authoring assistance (schema-aware, never semantics-defining)
